# 로컬 개발용 값입니다. 실제 비밀번호를 넣지 마십시오.
# docker compose --env-file .env.dev -f compose.yaml -f compose.dev.yaml
# Go 애플리케이션은 호스트에서 실행하므로 앱 변수는 README의 실행 명령에 있습니다.

# postgres
POSTGRES_PASSWORD=dev
POSTGRES_PORT=15432

# redis
REDIS_MAXMEMORY=128mb
REDIS_PORT=16379
